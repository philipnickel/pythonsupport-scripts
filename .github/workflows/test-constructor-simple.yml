name: Test Constructor Simple

on:
  workflow_dispatch:
  push:
    branches:
      - constructor-pkg-installer
    paths:
      - 'MacOS/constructor_installer/**'

jobs:
  test-simple:
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test basic setup
        run: |
          echo "=== Basic Test ==="
          pwd
          ls -la MacOS/constructor_installer/
          echo "✅ Basic test passed"