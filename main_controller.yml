# Main Controller Configuration
# Edit these values to enable/disable components

macOS:
  orchestrator: enabled    # First year student installation
  legacy: enabled          # 2024 legacy installation scripts
  pkg: enabled             # Offline installation package

windows:
  orchestrator: enabled    # Windows installation orchestrator
  legacy: enabled          # 2024 legacy installation scripts
  msi: enabled             # Offline installation package

# Global settings
maintenance_mode: disabled
maintenance_message: ""