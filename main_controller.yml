# Main Controller Configuration
# Edit these values to enable/disable components

macOS:
  orchestrator: disabled   # First year student installation
  legacy: disabled         # 2024 legacy installation scripts
  pkg: disabled            # Offline installation package

windows:
  orchestrator: disabled   # Windows installation orchestrator
  legacy: disabled         # 2024 legacy installation scripts
  msi: disabled            # Offline installation package

# Global settings
maintenance_mode: disabled
maintenance_message: ""